import React from 'react';
import {
  StyleSheet,
  Text,
  TextInput,AppRegistry,
  TouchableOpacity,
  View,Image
} from 'react-native';
import { Container,Content, Header, Tab, Tabs, TabHeading, Icon ,Thumbnail} from 'native-base';
import { Actions } from 'react-native-router-flux';
import SwipeCards from './SwipeCards.js'

export default class tabThree extends React.Component {
  state = {
    name: '',
  };
  render() {
    return (
      <Container>
      <Content>


</Content>
     </Container>
    );
  }
}
